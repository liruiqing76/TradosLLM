<UserControl x:Class="TradosToolkit.Workbench.TranslationCenterNavControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             FontFamily="Microsoft YaHei UI, Segoe UI" FontSize="12"
             Background="{DynamicResource RegionBrush}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/SkinDefault.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key="ToolButton" TargetType="Button" BasedOn="{StaticResource ButtonDefault}">
                <Setter Property="Margin" Value="0,0,0,4"/>
                <Setter Property="Padding" Value="6,5"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="FontSize" Value="12"/>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <DockPanel Margin="8,10,8,8">
        <TextBlock DockPanel.Dock="Top" Text="书签" FontWeight="Bold" FontSize="13"
                   Margin="2,0,0,8" Foreground="{DynamicResource PrimaryTextBrush}"/>
        <UniformGrid DockPanel.Dock="Top" Rows="1" Columns="3" Margin="0,0,0,8">
            <Button Content="＋目录" Style="{StaticResource ToolButton}" Margin="0,0,4,0"
                    Click="NewFolder_Click" ToolTip="在选中目录下新建子目录（未选中则建在根）"/>
            <Button Content="☆收藏" Style="{StaticResource ToolButton}" Margin="0,0,4,0"
                    Click="AddBookmark_Click" ToolTip="把浏览器当前页收藏进指定目录（弹窗选择）"/>
            <Button Content="⇩导出" Style="{StaticResource ToolButton}" Margin="0"
                    Click="Export_Click" ToolTip="导出全部书签为 HTML（Chrome/Edge 可直接导入）"/>
        </UniformGrid>
        <TextBlock DockPanel.Dock="Bottom" x:Name="NavStatus" FontSize="11" Margin="2,6,2,0"
                   TextWrapping="Wrap" Foreground="{DynamicResource SecondaryTextBrush}"/>
        <TreeView x:Name="BookmarkTree" BorderThickness="0"
                  Background="Transparent"/>
    </DockPanel>
</UserControl>
